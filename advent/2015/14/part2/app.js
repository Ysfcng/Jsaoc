const fs=require("fs")
let text=fs.readFileSync("puz.txt",{encoding:"UTF-8"}).split("\n").filter(x=>x!="")
let y=console.log

let reg=/(\S+)\s\S+(\d+)\D+(\d+)\D+(\d+)/
let  ob={}

for(let i=0;i<=i<duration;i++){

text.map(x=>{




})




}


y(text)
